let animate = gsap.timeline();

animate.from('.ins',{
    x : -2000,
})
animate.from('.glitches',{
    x : -2000,
})
animate.from('.start',{
    x : -2000,
})
animate.from('.g',{
    opacity:0,
    y : 20,
})
animate.from('.p',{
    opacity:0,
    x : -20,
})